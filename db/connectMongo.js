// import {MongoClient} from 'mongodb';

module.exports = {
    connect: async (uri) => {
        // MongoClient.connect(uri, {useNewUrlParser: true, useUnifiedTopology: true})
        //     .then((result) => result.db.people.find({}))
        //     .catch(e => console.log(e));
    },
    find: async () => {

    },
}
